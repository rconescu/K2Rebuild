version: "3.9"
services:
  k2rebuild:
    build: .
    image: k2rebuild:latest
    container_name: k2rebuild
    privileged: true
    volumes:
      - ./:/repo        # mount repo root
      - ./output:/repo/output  # explicit mount for results
    working_dir: /repo
    tty: true
    stdin_open: true
